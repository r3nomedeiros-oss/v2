{
  "buildCommand": "cd frontend && yarn build",
  "outputDirectory": "frontend/build",
  "installCommand": "cd frontend && yarn install",
  "devCommand": "cd frontend && yarn start",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://app-backend.vercel.app/api/:path*"
    }
  ]
}
